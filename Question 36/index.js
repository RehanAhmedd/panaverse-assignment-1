"use strict";
function make_shirt(size, message) {
    console.log(`The shirt is size ${size} and the message printed on it is "${message}".`);
}
// Call the function with sample arguments
make_shirt("XL", "Parada");
